//** Los componentes van en Mayusculas desde el principio, porque son componentes */
//** Es un High Order Component, osea como lo que se ha venido haciendo que tenga componentes dentro osea puedo colocar componentes hijos dentro de el */

import { createContext } from 'react';

export const UserContext = createContext(null);