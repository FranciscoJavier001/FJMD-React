const activo = true;

/** 
 * let mensaje = '';
 * 
 * if (!actio) {
 * mensaje = 'Activo';
 * } else {
 * mensaje = 'Inactivo';
 * }
 */

// const mensaje = (activo) ? 'Activo' : 'Inactivo'; //** Cuando se cunple ponemos el ? en caso que no se cumpla ponemos  */
// const mensaje = (activo) ? 'Activo' : null;
const mensaje = (activo === true) && 'Activo'

console.log(mensaje);QAZWSXEDC4rfv